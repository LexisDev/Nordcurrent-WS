## Installation
```bash
npm install
```

## Configuration
Copy file `.env.sample` to `.env` and fill it with configuration parameters

## Scripts
- compile typescript: `npm run build`
- run tslint: `npm run start`
- fix tslint errors: `npm run generate-docs`
- start application: `npm run build-docs`
- run tests: `npm run test`
